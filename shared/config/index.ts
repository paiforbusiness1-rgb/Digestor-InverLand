export { APP_CONFIG_DEFAULTS } from './defaults';
export type {
  IngestMode,
  RagMode,
  EmbeddingProvider,
  OcrProvider,
  GenerationProvider,
  PublicAppConfig,
  ServerAppConfig,
} from './schema';
/** loadServerConfig: importar desde '@/shared/config/loadServerConfig' solo en Express. */
